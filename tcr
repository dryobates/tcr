#! /bin/sh
# https://github.com/dryobates/tcr

set -euo pipefail

expected=${1:-}

case $expected in
    red)
        $TCR_TEST_COMMAND && exit 1 || exit 0
        ;;
    green)
        $TCR_TEST_COMMAND
        ;;
    *)
        echo "Usage: tcr {red,green} [msg]"
        exit 1
        ;;
esac
